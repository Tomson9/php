### **📌 QCM sur les Bases de PHP (20 Questions avec Réponses)**  

#### **📖 Instructions :**  
- Chaque question a **quatre choix** (a, b, c, d).  
- **Une seule bonne réponse** par question.  
- **Les réponses sont fournies après chaque question.**  

---

### **1. Quelle extension de fichier est utilisée pour les scripts PHP ?**  
a) `.html`  
b) `.js`  
c) `.php`  
d) `.css`  

---

### **2. Comment afficher "Hello World" en PHP ?**  
a) `print("Hello World");`  
b) `console.log("Hello World");`  
c) `echo "Hello World";`  
d) `System.out.println("Hello World");`  

---

### **3. Quel mot-clé est utilisé pour déclarer une variable en PHP ?**  
a) `var`  
b) `let`  
c) `$`  
d) `declare`  

---

### **4. Quelle est la sortie de `echo 5 + "3";` en PHP ?**  
a) `8`  
b) `53`  
c) `Erreur`  
d) `NaN`  

---

### **5. Comment insérer du code PHP dans un fichier HTML ?**  
a) `<php> echo "Bonjour"; </php>`  
b) `<?php echo "Bonjour"; ?>`  
c) `{php} echo "Bonjour"; {/php}`  
d) `<? echo "Bonjour"; ?>`  

---

### **6. Quelle fonction permet de connaître le type d’une variable ?**  
a) `getType()`  
b) `typeof()`  
c) `var_dump()`  
d) `typeOf()`  

---

### **7. Quelle superglobale est utilisée pour récupérer des données envoyées via un formulaire en méthode GET ?**  
a) `$_POST`  
b) `$_SESSION`  
c) `$_GET`  
d) `$_FORM`  

---

### **8. Comment déclarer une constante en PHP ?**  
a) `const NOM = "Valeur";`  
b) `define("NOM", "Valeur");`  
c) `constant NOM = "Valeur";`  
d) `let NOM = "Valeur";`  

---

### **9. Quelle fonction est utilisée pour obtenir une entrée utilisateur dans le terminal ?**  
a) `readline()`  
b) `get_input()`  
c) `fgets(STDIN)`  
d) `input()`  

---

### **10. Comment ajouter un commentaire sur une seule ligne en PHP ?**  
a) `/* Commentaire */`  
b) `# Commentaire`  
c) `// Commentaire`  
d) Les deux réponses (b) et (c)  

---

### **11. Que retourne la fonction `strlen("PHP")` ?**  
a) `2`  
b) `3`  
c) `4`  
d) `Erreur`  

---

### **12. Comment afficher du texte en PHP avec une nouvelle ligne ?**  
a) `echo "Hello\nWorld";`  
b) `echo "Hello World\n";`  
c) `echo "Hello" . PHP_EOL . "World";`  
d) Toutes les réponses ci-dessus  

---

### **13. Quelle fonction convertit une chaîne en majuscules ?**  
a) `uppercase()`  
b) `toUpperCase()`  
c) `strtoupper()`  
d) `strtouppercase()`  

---

### **14. Quelle fonction permet de supprimer les espaces en début et fin de chaîne ?**  
a) `trim()`  
b) `cut()`  
c) `removeSpaces()`  
d) `strip()`  

---

### **15. Quelle est la différence entre `==` et `===` en PHP ?**  
a) `==` compare la valeur seulement, `===` compare la valeur et le type  
b) `===` est une version plus rapide de `==`  
c) `==` compare deux objets, `===` compare deux tableaux  
d) Aucune différence  

---

### **16. Quelle superglobale contient les variables de session ?**  
a) `$_SESSION`  
b) `$_SERVER`  
c) `$_GLOBAL`  
d) `$_VAR`  

---

### **17. Comment vérifier si une variable `$nom` est définie ?**  
a) `is_set($nom)`  
b) `isset($nom)`  
c) `defined($nom)`  
d) `exists($nom)`  

---

### **18. Quelle est la sortie de `echo 2 . 3;` ?**  
a) `5`  
b) `23`  
c) `6`  
d) `Erreur`  

---

### **19. Quelle méthode est utilisée pour inclure un fichier dans un autre en PHP ?**  
a) `import()`  
b) `require()`  
c) `include()`  
d) Les deux réponses (b) et (c)`  

---

### **20. Quelle est la sortie de `echo (int) "5.7";` ?**  
a) `5.7`  
b) `6`  
c) `5`  
d) `Erreur`  

---

## **📌 Score :**
- **18-20 points** 🏆 : Excellent, tu maîtrises bien les bases de PHP !  
- **14-17 points** 👍 : Très bien, continue à pratiquer !  
- **10-13 points** ✅ : Bon début, révise un peu plus les bases.  
- **Moins de 10 points** ❗ : Révise et pratique davantage.  

---

💡 **Good Luck** 🚀